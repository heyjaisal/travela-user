export const API_BASE_URL = import.meta.env.VITE_SERVER_URL

export const Host = "http://localhost:5000"

import { BotMessageSquareIcon, MapPinnedIcon, HomeIcon, MessageSquareIcon, QrCodeIcon, UsersIcon } from "lucide-react";

import animationData from '@/asset/lottie-json.json'

export const animation = {
    loop:true,
    autoplay:true,
    animationData:animationData
}

  







